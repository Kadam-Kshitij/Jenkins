pipeline {
    agent {
        docker { image 'ubuntu:20.04' }
    }
    stages {
        stage('Build') {
            steps {
                sh 'ls'
                sh 'df -h'
            }
        }
    }
}
