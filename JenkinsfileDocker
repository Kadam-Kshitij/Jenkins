pipeline {
    agent {
        docker { image 'ubuntu-cpp:latest' }
    }
    stages {
        stage('Build') {
            steps {
                sh 'echo $PWD'
                sh 'df -h'
                sh 'ls /home'
                sh 'which gcc'
            }
        }
    }
}
