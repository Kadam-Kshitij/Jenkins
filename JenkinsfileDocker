pipeline {
    agent {
        docker { image 'ubuntu-cpp:latest' }
    }
    stages {
        stage('Build') {
            steps {
                sh 'g++ -o exe Hello.cpp'
                sh './exe'
                sh 'mkdir -p /home/hello'
                sh 'cp -r ./exe /home/hello'
            }
        }
    }
}
