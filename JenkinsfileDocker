pipeline {
    agent {
        docker { image 'ubuntu-cpp:latest' }
    }
    stages {
        stage('Build') {
            steps {
                sh 'gcc -o exe Hello.cpp'
                sh './exe'
                sh 'mkdir hello'
                sh 'cp -r ./exe /home/hello'
            }
        }
    }
}
