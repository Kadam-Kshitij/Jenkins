pipeline {
    agent {
        docker { 
            image 'ubuntu-cpp:latest'
            args '-v /home/ubuntu/jenkinsBuild:/home/hello'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'g++ -o exe Hello.cpp'
                sh './exe'
                sh 'sudo mkdir -p /home/hello'
                sh 'sudo cp -r ./exe /home/hello'
            }
        }
    }
}
